manifest_version = "1"
id = "mqtt-broker"
description = "MQTT Message Broker for Spin Message Trigger"
tags = ["messaging", "rust"]

[add_component.snippets]
component = "component.txt"

[parameters]
broker-url = { type = "string", prompt = "Broker Url", default = "mqtt://mqtt:1883" }
